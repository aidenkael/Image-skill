# AGENTS.md — Agent 行为约束

本文件约束所有参与 Image Skill 项目的 AI Agent 行为。

## 绝对禁止

- **不要** 开发完整 GUI 软件
- **不要** 自建图像生成算法或训练模型
- **不要** 大规模部署 ComfyUI 或下载大量模型到本仓库
- **不要** 建立复杂数据库或任务队列
- **不要** 自动发布到任何电商平台
- **不要** 为了"架构漂亮"重写已有成熟能力
- **不要** 为了未来可能需要的功能提前开发
- **不要** 将 API Key 写入代码或提交到 Git
- **不要** 将生成图片、缓存、大模型文件提交到 Git

## 必须遵守

- 优先复用已有开源 Skill / Prompt / Workflow
- 只有明确缺少的部分才自行补充
- 模型接口尽量可替换（不写死单一 Provider）
- API Key 只通过 `.env` 管理
- 每次修改后检查 `.gitignore` 是否覆盖新的大文件目录
- 提交前检查无密钥泄露

## 评价标准

不以"技术复杂度"或"代码量"评价方案好坏。唯一评价标准：

> 最终图片质量 + 商品真实性 + 真人自然度 + 去 AI 味 + 使用效率 + 长期成本

## 三条路线优先级

1. **方案 A**（当前重点）：Agent + Skill + 图片模型 API
2. **方案 B**（资料预留）：ComfyUI 工作流收集
3. **方案 C**（持续跟踪）：商业软件评测

## 目录使用规则

| 目录 | 用途 | 提交 Git |
|------|------|----------|
| `skills/` | Agent Skill 文件 | ✅ |
| `prompts/` | Prompt 模板 | ✅ |
| `workflows/` | ComfyUI 工作流 JSON | ✅ |
| `configs/` | 模型配置 | ✅（不含密钥） |
| `references/` | 参考资料 | ✅ |
| `tests/` | 测试用例描述 | ✅ |
| `tests/samples/` | 测试商品原图 | ✅（小文件） |
| `docs/` | 文档 | ✅ |
| `outputs/` | 生成结果 | ❌ |

## 开发节奏

- 完成一个有意义阶段后再 Commit，不要为每个小文件单独 Commit
- Commit message 用中文，简明扼要
- 不要一次做太多事情，保持每次提交可审查
